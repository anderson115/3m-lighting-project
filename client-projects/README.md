# Client Project Folders

Each subfolder corresponds to a distinct project or engagement. Current naming convention uses `yymmdd-client-workstream` (for example, `251106-3m-accent-lighting`).

Archived engagements live under `archive/`.
