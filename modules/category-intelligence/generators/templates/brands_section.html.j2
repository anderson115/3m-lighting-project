<h2>Brand Landscape & Market Share</h2>

<p><strong>Coverage:</strong> {{ brands_count }} brands identified across 5 market tiers, from national leaders (>$500M) to import and niche players. Market share estimates validated through multi-source triangulation.</p>

<table>
    <thead>
        <tr>
            <th>Brand</th>
            <th>Tier</th>
            <th>Market Share</th>
            <th>Category Revenue</th>
            <th>Market Position</th>
            <th>Confidence</th>
        </tr>
    </thead>
    <tbody>
        {% for brand in brands %}
        <tr>
            <td><strong>{{ brand.name }}</strong></td>
            <td><span class="tier tier-{{ brand.tier_num }}">T{{ brand.tier_num }}</span></td>
            <td>{{ brand.market_share }}</td>
            <td>{{ brand.revenue }}</td>
            <td>{{ brand.position }}</td>
            <td><span class="confidence confidence-{{ brand.confidence }}">{{ brand.confidence|title }}</span></td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% if competitive_factors or emerging_threats %}
<div class="two-col" style="margin-top: 12pt;">
    {% if competitive_factors %}
    <div>
        <h4>Key Competitive Factors</h4>
        <ul>
            {% for factor in competitive_factors %}
            <li>{{ factor }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}
    {% if emerging_threats %}
    <div>
        <h4>Emerging Threats</h4>
        <ul>
            {% for threat in emerging_threats %}
            <li>{{ threat }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}
</div>
{% endif %}
