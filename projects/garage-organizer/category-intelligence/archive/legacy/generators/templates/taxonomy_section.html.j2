<h2>Category Structure & Taxonomy</h2>

<p><strong>Category Overview:</strong> {{ subcategories_count }} primary subcategories identified with comprehensive quantitative metrics including market size, unit volumes, brand counts, and growth rates.</p>

{% if consumer_keywords or industry_keywords %}
<div class="keywords">
    {% if consumer_keywords %}
    <strong>Consumer Language:</strong>
    {% for kw in consumer_keywords %}
    <span class="keyword">{{ kw }}</span>
    {% endfor %}
    <br>
    {% endif %}
    {% if industry_keywords %}
    <strong>Industry Language:</strong>
    {% for kw in industry_keywords %}
    <span class="keyword industry">{{ kw }}</span>
    {% endfor %}
    {% endif %}
</div>
{% endif %}

<table>
    <thead>
        <tr>
            <th>Subcategory</th>
            <th>Market Size</th>
            <th>% of Category</th>
            <th>Units/Year</th>
            <th>Brands</th>
            <th>Avg Price</th>
            <th>Growth</th>
        </tr>
    </thead>
    <tbody>
        {% for subcat in subcategories %}
        <tr>
            <td><strong>{{ subcat.name }}</strong></td>
            <td>{{ subcat.market_size }}</td>
            <td>{{ subcat.category_share }}</td>
            <td>{{ subcat.units }}</td>
            <td>{{ subcat.brands }}</td>
            <td>{{ subcat.avg_price }}</td>
            <td>{{ subcat.growth }}</td>
        </tr>
        {% if subcat.keywords %}
        <tr style="background: #fafafa;">
            <td colspan="7" style="color: #666;">
                <em>Keywords:</em> {{ subcat.keywords }}
            </td>
        </tr>
        {% endif %}
        {% endfor %}
    </tbody>
</table>
