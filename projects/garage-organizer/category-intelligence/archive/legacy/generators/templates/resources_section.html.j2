{% if resource_categories %}
<h2>Appendix: Key Resources & References</h2>

<p><em>Total of {{ total_resources }} curated sources across {{ categories_count }} categories. Top resources shown below for space efficiency. All sources validated for authority, relevance, and recency.</em></p>

{% for category in resource_categories %}
<div class="compact-section">
    <h4>{{ category.name }}</h4>
    <table>
        <thead>
            <tr>
                <th>Resource</th>
                <th>Provider</th>
                <th>URL</th>
                <th>Relevance</th>
            </tr>
        </thead>
        <tbody>
            {% for resource in category.resources %}
            <tr>
                <td><strong>{{ resource.title }}</strong></td>
                <td>{{ resource.provider }}</td>
                <td><a href="{{ resource.url }}" target="_blank" style="color: #0066cc; text-decoration: none;">{{ resource.url[:50] }}...</a></td>
                <td>{{ resource.relevance }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endfor %}
{% endif %}
